export function startSignalState(state={}){const status=state.status;if(status==='PAUSED')return null;if(status==='COUNTDOWN')return{red:Math.max(1,Math.min(5,Math.floor((3-Math.max(0,state.countdown||0))/3*5)+1)),green:false};return{red:0,green:status==='RUNNING'&&(state.totalTime||0)<3};}
export function createRaceStartSignal(T,{scene}){
 const root=new T.Group();root.name='V7_DiegeticStartingSignal';root.userData.visualOnly=true;root.visible=false;scene.add(root);const geometries=new Set(),materials=new Set(),lights=[];let chart=0,last={red:0,green:false};
 const steel=new T.MeshStandardMaterial({color:0x333e41,roughness:.46,metalness:.74}),black=new T.MeshStandardMaterial({color:0x101617,roughness:.78}),concrete=new T.MeshStandardMaterial({color:0x85847a,roughness:.96});materials.add(steel);materials.add(black);materials.add(concrete);
 function mesh(name,g,m,x,y,z){geometries.add(g);materials.add(m);const item=new T.Mesh(g,m);item.name=name;item.position.set(x,y,z);item.castShadow=true;item.receiveShadow=true;root.add(item);return item;}
 let built=false;function build(width){root.clear();for(const lens of lights){lens.material.dispose();materials.delete(lens.material);}lights.length=0;for(const g of geometries)g.dispose();geometries.clear();const half=width/2+1.1,height=4.7;
  for(const side of [-1,1]){mesh('ConcreteFooting',new T.BoxGeometry(.64,.18,.68),concrete,side*half,.09,0);mesh('AnchoredBasePlate',new T.BoxGeometry(.39,.028,.4),steel,side*half,.194,0);mesh('SteelPost',new T.CylinderGeometry(.1,.1,height-.2,12),steel,side*half,(height+.2)/2,0);for(const a of [-1,1])for(const b of [-1,1])mesh('AnchorBolt',new T.CylinderGeometry(.017,.017,.047,6),steel,side*half+a*.14,.231,b*.145);}
  mesh('SteelCrossBeam',new T.BoxGeometry(half*2+.2,.26,.21),steel,0,height,0);
  mesh('SignalHousing',new T.BoxGeometry(2.62,.72,.28),black,0,height-.16,.18);
  for(let i=0;i<5;i++){const x=(i-2)*.49;mesh('LensRecess',new T.CylinderGeometry(.224,.224,.06,24),black,x,height-.16,.35).rotation.x=Math.PI/2;const material=new T.MeshStandardMaterial({color:0x29120e,emissive:0x000000,roughness:.32,metalness:.12});const lens=mesh('StartLens_'+(i+1),new T.SphereGeometry(.183,24,12,0,Math.PI*2,0,Math.PI/2),material,x,height-.16,.39);lens.rotation.x=Math.PI/2;lights.push(lens);mesh('LightVisor',new T.BoxGeometry(.43,.05,.37),black,x,height+.095,.48);}
  built=true;
 }
 function set(route){if(!route?.frame||!Array.isArray(route.position))throw Error('Physical route frame required for start signal');const width=Math.max(3,Math.min(20,route.widthM));build(width);const left=new T.Vector3().fromArray(route.frame.left),normal=new T.Vector3().fromArray(route.frame.normal),back=new T.Vector3().fromArray(route.frame.tangent).negate();root.quaternion.setFromRotationMatrix(new T.Matrix4().makeBasis(left,normal,back));root.position.fromArray(route.position);root.updateMatrixWorld(true);chart=route.referenceChart||0;root.visible=true;return diagnostics();}
 function update(state){if(!built)return;root.visible=!['IDLE','FINISHED'].includes(state.status);const value=startSignalState(state);if(value)last=value;for(let i=0;i<5;i++){const lit=last.green||i<last.red,colour=last.green?0x24ec72:0xfa301e;lights[i].material.color.setHex(lit?colour:0x291511);lights[i].material.emissive.setHex(lit?colour:0);lights[i].material.emissiveIntensity=lit?2.7:0;}}
 function rebase(event){if(!built)return;root.updateMatrix();root.applyMatrix4(new T.Matrix4().fromArray(event.matrix));root.updateMatrixWorld(true);chart=event.referenceChart;}
 function diagnostics(){return{built,referenceChart:chart,visible:root.visible,lights:{...last},position:root.position.toArray(),clearanceM:4.18,visualOnly:true,colliders:0};}
 return{set,update,rebase,diagnostics,clear(){root.visible=false;},dispose(){root.removeFromParent();for(const g of geometries)g.dispose();for(const m of materials)m.dispose();root.clear();}};
}
