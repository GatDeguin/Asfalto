import {installSurfaceRelief,configureSurfaceRelief,setSurfaceReliefQuality,surfaceReliefDiagnostics} from '../src/tracks/visuals/surface-relief.mjs';
import {improveRegionalRoadMaterials} from '../src/tracks/visuals/regional-road-surfaces.mjs';
export function reliefOnly(){qaRoad.restore();const T=__chevyV6Three;configureSurfaceRelief(T);setSurfaceReliefQuality('low');for(const e of qaRoadMeshes){const m=e.material,n=new T.MeshStandardMaterial({map:m.map,normalMap:m.normalMap,normalScale:m.normalScale,roughnessMap:m.roughnessMap,roughness:m.roughness,metalness:0,color:m.color,envMapIntensity:m.envMapIntensity,side:m.side});if(m.asfaltoHeightTexture)installSurfaceRelief(n,{heightMap:m.asfaltoHeightTexture,metres:9,depthM:.008,projection:'uv'});e.mesh.material=n;}__cockpit.render();return surfaceReliefDiagnostics();}
export function retainedSourceMap(){qaRoad.restore();for(const e of qaRoadMeshes){const m=e.material;if(m.asfaltoRetiredTexture0){globalThis.qaSavedMap??=new Map();qaSavedMap.set(m,m.map);m.map=m.asfaltoRetiredTexture0;m.needsUpdate=true;}}__cockpit.render();}
