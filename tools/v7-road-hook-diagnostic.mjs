export function variant(kind){qaRoad.restore();for(const e of qaRoadMeshes){const old=e.material,mat=old.clone();mat.onBeforeCompile=function(shader,renderer){old.onBeforeCompile.call(this,shader,renderer);if(kind==='uv-alias')shader.fragmentShader=shader.fragmentShader.replace('#define vMapUv anReliefUV','').replace('#define vRoughnessMapUv (vRoughnessMapUv + anReliefUV - vMapUv)','').replace('#define vNormalMapUv (vNormalMapUv + anReliefUV - vMapUv)','');if(kind==='weather-normal')shader.fragmentShader=shader.fragmentShader.replace(/float anHeight=.*?(?=#include <clearcoat_normal_fragment_begin>)/s,'');if(kind==='road-color')shader.fragmentShader=shader.fragmentShader.replace(/float roadStation=.*?(?=#include <alphamap_fragment>)/s,'');};mat.customProgramCacheKey=()=>old.customProgramCacheKey.call(old)+'|qa-strip-'+kind;mat.needsUpdate=true;e.mesh.material=mat;}__cockpit.render();}
